{
    "name": "@indieport/database",
    "version": "0.0.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc",
        "type:check": "tsc --noEmit",
        "db:generate": "drizzle-kit generate",
        "db:push": "drizzle-kit push",
        "db:migrate": "drizzle-kit migrate",
        "test": "bun test",
        "test:coverage": "bun test --coverage"
    },
    "dependencies": {
        "@indieport/shared-types": "workspace:*",
        "drizzle-orm": "^0.41.0",
        "postgres": "^3.4.0"
    },
    "devDependencies": {
        "@types/node": "^25.6.0",
        "drizzle-kit": "^0.30.0",
        "typescript": "^5.8.0"
    }
}
